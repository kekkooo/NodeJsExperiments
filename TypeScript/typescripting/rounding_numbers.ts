/**
 * Startup
 */
class Startup {
    public static main() :number{
        var roundUp = 1.5;                
        console.log( Math.round( roundUp ));
        return 0;        
    }
}

Startup.main();