/**
 * Startup
 */
class Startup {
    public static main() :number{
        var example = 'example string';
        var str_length : number;
        str_length = example.length;
        console.log(str_length);
        return 0;        
    }
}

Startup.main();