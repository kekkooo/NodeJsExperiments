class Startup {
    public static main() :number{        
        var eat = (food:string ) => food + ' tasted really good.';
        console.log( eat('bananas') );
        return 0;        
    }
}
Startup.main();