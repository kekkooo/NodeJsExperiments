class Startup {
    public static main() :number{        
        var math = (a:number, b:number, c:number) => (b*c) + a;
        console.log( math(53, 61, 67) );
        return 0;        
    }
}
Startup.main();